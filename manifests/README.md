# Manifests

Store file inventories, hashes, build records, generated indexes, provenance maps, and other machine-readable manifests here. JSON, CSV, YAML, TOML, TSV, and plain-text formats are all acceptable.
